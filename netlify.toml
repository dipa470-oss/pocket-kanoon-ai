[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  LOVABLE_CONTEXT = "netlify"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
